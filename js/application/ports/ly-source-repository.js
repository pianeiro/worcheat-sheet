/**
 * Port: fetches LySource blobs from the Catalog.
 * Implemented by concrete adapters (e.g. FileLySourceRepository).
 *
 * @typedef {Object} LySourceRepository
 * @property {function(string, string): Promise<string>} fetchLy
 */
export {};
